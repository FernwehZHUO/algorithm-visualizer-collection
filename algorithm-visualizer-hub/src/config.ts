// Configuration settings for the algorithm visualizer hub
// In a production environment, these would be actual URLs to the deployed applications

// Development URLs (running locally)
const config = {
  // URL for the Max Flow application
  maxFlowUrl: 'http://localhost:3001',
  
  // URL for the Maximum Matching application
  maximumMatchingUrl: 'http://localhost:3002',
};

export default config; 